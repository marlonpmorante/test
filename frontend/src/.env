# ----------------------------------------------------------------------
# Configuration for Frontend (React, Vue, etc.)
# IMPORTANT: Variables here are exposed to the public/browser.
# DO NOT store private keys or secrets here.
# ----------------------------------------------------------------------

# NOTE: The prefix (e.g., REACT_APP_, VITE_, NEXT_PUBLIC_) is crucial
# and depends on your specific frontend framework/build tool.

# 1. API Configuration
REACT_APP_API_BASE_URL=http://localhost:5000/api
REACT_APP_BACKEND_URL=rbgonzalez-backend-production.up.railway.app
REACT_APP_TIMEOUT_MS=15000

# 2. Public Service Keys (e.g., Google Maps, Firebase Public Config)
REACT_APP_GOOGLE_MAPS_KEY=http://rbgonzales-pharmacy-production-a7b7.up.railway.app
REACT_APP_FIREBASE_PROJECT_ID=pharm-inventory-app

# 3. Environment/Mode (Usually handled by the build tool, but can be set)
# REACT_APP_ENV=development

# 4. Feature Flags
REACT_APP_ENABLE_BETA_FEATURES=false